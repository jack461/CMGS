var id = "T11";

var str = stralloc();

var w2 = sprintf(0, "%s  started", id); GM_Log(w2);


var res = event(`wait, `MIDI );

sprintf(str, "%s  event => %s", id, edn(0, res)); GM_Log(str);

sprintf(str, "%s  ended", id); GM_Log(str);

