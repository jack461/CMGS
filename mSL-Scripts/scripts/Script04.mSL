var start = ptime();
var id = "S04";
var str=stralloc();

sprintf(str, "%s-%d-Start.", id, thread(`idt));
GM_Log(str);

var siz = 4000;
var Tab = malloc(siz);
var i, ent;
i=0;
loop (400,
    loop (1000,
        ent = 128 + irand(888);
        Tab[i] = malloc(ent);
        i = (i+1) % siz;
    );
);
sprintf(str, "%s-%d-Done.  %.2fs", id, thread(`idt), ptime() - start);
GM_Log(str);
strfree(str);

